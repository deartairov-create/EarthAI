"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
export default function Login(){const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");const r=await fetch("/api/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});if(r.ok){router.replace("/");router.refresh();}else{setError("Wrong password");setBusy(false);}}return <main className="login-page"><form className="login-card" onSubmit={submit}><div className="login-orb">◎</div><div className="eyebrow">PRIVATE WORLD</div><h1>AI Earth Ultra</h1><p>Enter the password for your private autonomous planet.</p><input autoFocus type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><button disabled={busy}>{busy?"Opening world…":"Enter world"}</button>{error&&<div className="error">{error}</div>}<small>Default local password: earthai — change APP_PASSWORD on Vercel.</small></form></main>}
